import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  semanticTokens: {
    colors: {
      primary: "#26273B",
      secondary: "#EA624A",
      header: "#fff",
      body: "#A2ABA3",
    },
  },
});

export default theme;
